# Database Indexes

> Auto-generated by `npm run update-schema` — 2026-02-08T12:10:30.534Z

| Index | Table | Columns | Unique | Primary |
|-------|-------|---------|--------|---------|
| daily_stats_pkey | daily_stats | id | YES | YES |
| daily_stats_store_id_date_key | daily_stats | store_id, date | YES |  |
| message_logs_pkey | message_logs | id | YES | YES |
| message_templates_pkey | message_templates | id | YES | YES |
| idx_opex_store | operational_expenses | store_id, start_date, end_date |  |  |
| operational_expenses_pkey | operational_expenses | id | YES | YES |
| idx_orders_delivery_mode | orders | delivery_mode |  |  |
| orders_pkey | orders | id | YES | YES |
| idx_pricelist_feeds_store | pricelist_feeds | store_id |  |  |
| idx_pricelist_feeds_token | pricelist_feeds | feed_token |  |  |
| pricelist_feeds_feed_token_key | pricelist_feeds | feed_token | YES |  |
| pricelist_feeds_pkey | pricelist_feeds | id | YES | YES |
| pricelist_feeds_store_id_key | pricelist_feeds | store_id | YES |  |
| products_pkey | products | id | YES | YES |
| stores_pkey | stores | id | YES | YES |
| subscriptions_pkey | subscriptions | id | YES | YES |
| users_email_key | users | email | YES |  |
| users_pkey | users | id | YES | YES |

---

```sql
CREATE UNIQUE INDEX daily_stats_pkey ON public.daily_stats USING btree (id);
```

```sql
CREATE UNIQUE INDEX daily_stats_store_id_date_key ON public.daily_stats USING btree (store_id, date);
```

```sql
CREATE UNIQUE INDEX message_logs_pkey ON public.message_logs USING btree (id);
```

```sql
CREATE UNIQUE INDEX message_templates_pkey ON public.message_templates USING btree (id);
```

```sql
CREATE INDEX idx_opex_store ON public.operational_expenses USING btree (store_id, start_date, end_date);
```

```sql
CREATE UNIQUE INDEX operational_expenses_pkey ON public.operational_expenses USING btree (id);
```

```sql
CREATE INDEX idx_orders_delivery_mode ON public.orders USING btree (delivery_mode);
```

```sql
CREATE UNIQUE INDEX orders_pkey ON public.orders USING btree (id);
```

```sql
CREATE INDEX idx_pricelist_feeds_store ON public.pricelist_feeds USING btree (store_id);
```

```sql
CREATE INDEX idx_pricelist_feeds_token ON public.pricelist_feeds USING btree (feed_token);
```

```sql
CREATE UNIQUE INDEX pricelist_feeds_feed_token_key ON public.pricelist_feeds USING btree (feed_token);
```

```sql
CREATE UNIQUE INDEX pricelist_feeds_pkey ON public.pricelist_feeds USING btree (id);
```

```sql
CREATE UNIQUE INDEX pricelist_feeds_store_id_key ON public.pricelist_feeds USING btree (store_id);
```

```sql
CREATE UNIQUE INDEX products_pkey ON public.products USING btree (id);
```

```sql
CREATE UNIQUE INDEX stores_pkey ON public.stores USING btree (id);
```

```sql
CREATE UNIQUE INDEX subscriptions_pkey ON public.subscriptions USING btree (id);
```

```sql
CREATE UNIQUE INDEX users_email_key ON public.users USING btree (email);
```

```sql
CREATE UNIQUE INDEX users_pkey ON public.users USING btree (id);
```

